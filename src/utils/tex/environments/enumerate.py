from typing import TextIO


def enumerate(start: int, out_file: TextIO) -> int:
    """For enumerate/lists environment.

    Args:
        out_file -- where the output will be written.

    Returns:
        The index of line where the enumerate ends.
    """
    ...
